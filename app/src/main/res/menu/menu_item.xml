<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">


    <!--
        showAsAction : 어떻게 보여지는지.
        always : 액션바에 항상 표시
        neaver : 액션바 항목에 표시 하지 않는다.
        ifroom : 액션바에 여유 공간이 없는 경우에는 추가하지 않는다. 즉 여유 공간이 있을 때만 추가.
        withText : 오른쪽에 ...이 생기며 거기에 text로 들어간다.
        collapseActionView : 아이템에 설정한 뷰에 아이콘만 설정.
        orderInCategory : 아이템의 순서
    -->
    <item
        android:id="@+id/item01"
        android:title="menu1"
        android:orderInCategory="2"
        android:icon="@android:drawable/ic_lock_idle_low_battery"
        app:showAsAction="always"/>

    <item
        android:id="@+id/item02"
        android:title="menu2"
        android:orderInCategory="3"
        android:icon="@android:drawable/ic_dialog_email"
        app:showAsAction="always"/>
    <item
        android:id="@+id/item03"
        android:title="menu3"
        android:orderInCategory="1"
        android:icon="@android:drawable/ic_dialog_info"
        app:showAsAction="always"/>

</menu>